/* eslint-disable @typescript-eslint/no-explicit-any */
export type SubmissionPayload = {
  outputCase: any;
  code: string;
  language: string;
  inputCase: string;
  userId: string;
  submissionId: string;
};
